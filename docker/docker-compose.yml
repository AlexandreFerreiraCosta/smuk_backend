version: '3.1'

services:

  db:
    image: postgres
    container_name: "postgres"
    environment:
      POSTGRES_DB: smuk
      POSTGRES_USER: smuk
      POSTGRES_PASSWORD: smuk
    volumes:
      - "./data/postgres:/var/lib/postgresql/data"
    ports:
      - 5432:5432
